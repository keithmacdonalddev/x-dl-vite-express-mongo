---
name: purge-context
description: >
  Alias for /secondpass. Comprehensive end-of-session handoff generator using Sonnet.
  Use /secondpass directly — this skill exists for backward compatibility.
argument-hint: ''
allowed-tools: Read, Write, Bash, Glob, Grep
context: main
model: sonnet
---

# Purge Context (Alias for /secondpass)

This skill is an alias. Run `/secondpass` instead for the canonical session handoff generator.

If invoked directly, follow the exact same process as defined in `.claude/skills/secondpass/skill.md`.
