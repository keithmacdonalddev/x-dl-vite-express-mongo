# client/src/ — Agent Working Directory

This is the valid working directory for client agents. The client has no sub-domains — all client source files are owned by client agents.

See `client/CLAUDE.md` (parent directory) for full architecture, state management rules, and coding patterns.
